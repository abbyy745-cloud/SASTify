for backend : go to the backend directory and open cmd
             1) python -m pip install -r requirements.txt
             2) python -m uvicorn main:app --reload --host 0.0.0.0 --port 8000


for vscode-extensio: go to the vs-code and open the sastify project folder and then go to run and debug and there you will notice a green play button with a dropdown list for backend, front end, extension and fullstack. start the full stack and let it start. and then start the extension command and when the npm run compile command automatically goes through, a new window with the the title extension development host will launch where you can open files( open file) or open folders (add folder) from the side bar. once you have opened your desired folder or file, press Ctrl+shift+p and this will open the commands for you. type in sastify and all the sastify commands will show up and then use ur desired command and test it out
             